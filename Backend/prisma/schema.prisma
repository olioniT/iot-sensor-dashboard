generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Sensor { 
  id    Int     @id @default(autoincrement()) 
  name  String  @unique
  temp  Int
  location String
  activestate Boolean @default(false)
} 

model User { 
  id        Int     @id @default(autoincrement()) 
  name String
  admin Boolean @default(false)
  password String
} 